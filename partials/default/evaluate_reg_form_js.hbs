
<script>

// document.getElementById("reg_form").addEventListener("submit", function(event){
//   event.preventDefault()
// })

// function evaluate_reg_form() {

// 	var data = {
// 		profile: {
// 			firstName: $("#first_name").val(),
// 			lastName: $("#last_name").val(),
// 			email: $("#email").val(),
// 			login: $("#email").val()
// 		},
// 		credentials: {
// 			password: {
// 				value: $("#password").val()
// 			}
// 		}
// 	}

// 	var settings = {
// 		"url": "/register_via_api",
// 		"method": "POST",
// 		"headers": {
// 			"Accept": "application/json",
// 			"Content-Type": "application/json",
// 		},
// 		"data": JSON.stringify(data)
// 	}

// 	$.ajax(settings).done(function (sessionToken) {
// 		// alert("received a response!")
// 		console.log("the session token is: " + sessionToken)

// 		var url = "{{OKTA_TENANT}}" + "/login/sessionCookieRedirect?token="
// 		url += sessionToken
// 		url += "&redirectUrl=" + "REDIRECT_URI" 

// 		window.location.href = data.redirect

// 	})

// 	return false

// Location: https://okta.okta.com/login/sessionCookieRedirect?token=0HsohZYpJgMSHwmL9TQy7RRzuY&redirectUrl=https%3A%2F%2Fwww.example.com%2Fportal%2Fhome



	// 	var signIn = new OktaSignIn({
	// 		baseUrl: '{{OKTA_TENANT}}',
	// 		sessionToken: sessionToken
	// 	})

	// 	signIn.session.get(function (res) {

	// 		console.log("the result of the session.get call is:")
	// 		console.dir(res)

	// 		// Session exists, show logged in state.
	// 		if (res.status === 'ACTIVE') {
	// 			console.log("the user has a session")
	// 			update_ui_logged_in(res.login)
	// 		}
	// 		// No session, or error retrieving the session. Render the Sign-In Widget.
	// 		else if (res.status === 'INACTIVE') {

	// 			console.log("the user does not have a session")

	// 			// $("#authn").show()
	// 			// $("#widget-container").show()

	// 			update_ui_logged_out()

	// 			signIn.renderEl(
	// 				{ el: '#widget-container' },
	// 				function success(res) {
	// 					res.session.setCookieAndRedirect("{{REDIRECT_URI}}")
	// 					update_ui_logged_in()

	// 				},
	// 				function error(err) {
	// 					console.log("something went wrong with the login effort")
	// 				}
	// 			)
	// 		}
	// 	});
	// })
// }
</script>
